import Foundation

/**
 * 1. Create an assertion to check the result of a function call 
 * `isPalindrome()` with the parameter "level" returns `true`.
 * 2. Create an assertion to check the result of a function call 
 * `isPalindrome()` with the parameter "Was it a car or a cat I saw?" returns `true`.
 */


print("Tests passed!")
